#include <bits/stdc++.h>
 
using namespace std;
 
 
int main() {
    int tiles, result;
    cin >> tiles;
    result = sqrt (tiles);
    cout << "The largest square has side length " << result << "." << endl;
    return 0;
}